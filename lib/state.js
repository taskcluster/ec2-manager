// Understand how to handle state changes
class State {
  constructor() { }

  instanceStateChange({region, id, newState}) { }

  spotRequestStateChagne({region, id, newState}) { }
}

module.exports = State;
